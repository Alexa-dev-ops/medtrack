name: medtrack_app
description: MedTrack - Smart Medication Reminder Application
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.3
  intl: ^0.20.2
  device_preview: ^1.3.1
  provider: ^6.1.5+1
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
